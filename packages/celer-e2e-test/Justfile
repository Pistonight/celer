lint:
    pylint scripts

build:
    @just ../../buildrs --release

test:
    @just run-test test_new

run-test TEST:
    python3 scripts/run_test.py ../../target/release/celer-devtool {{TEST}}

alias r:=run_test
